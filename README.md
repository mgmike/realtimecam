C++ implementation to feed webcam footage through a Yolo3 to detect objects. 

To Implement:
cd ./build
cmake ../src
cmake --build .

To debug, cd ./debug
cmake -DCMAKE_BUILD_TYPE=Debug ../src
cmake --build .